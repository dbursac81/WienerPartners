@page "/"
@using WienerPartners.Web.Shared
@inherits IndexBase

<Shared.NavMenu />

<div class="mb-3">
    <a class="btn btn-success" href="/create-partner">New partner</a>
</div>

<table class="table table-striped">
    <thead>
        <tr>
            <th></th>
            <th>Full name</th>
            <th>Partner number</th>
            <th>Partner type</th>
            <th>Created</th>
            <th>Policies count</th>
            <th>Policies total sum</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @if (Partners is null)
        {
            <tr><td colspan="8">Učitavanje...</td></tr>
        }
        else
        {
            foreach (var p in Partners)
            {
                var mark = (p.PoliciesCount > 5 || p.PoliciesSum > 5000m) ? "*" : "";
                var css = (p.Id == NewId) ? "highlight-new" : "";
                <tr class="@css" style="cursor:pointer" @onclick="() => ShowDetails(p.Id)">
                    <td>@mark</td>
                    <td>@p.FullName</td>
                    <td>@p.PartnerNumber</td>
                    <td>@(p.PartnerTypeId==1?"Personal":"Legal")</td>
                    <td>@p.CreatedAtUtc.ToString("yyyy-MM-dd HH:mm")</td>
                    <td>@p.PoliciesCount</td>
                    <td>@p.PoliciesSum</td>
                    <td>
                        <button class="btn btn-sm btn-primary" @onclick:stopPropagation="true" @onclick="() => OpenAddPolicy(p.Id)">New policy</button>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>

<PartnerDetailsModal Show="@ShowModal"
                     Partner="@SelectedPartner"
                     OnClose="@HideModal" />

